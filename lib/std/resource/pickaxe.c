inherit STD_ITEM;

void create () {
    ::create();
    set_name("pickaxe");
    set_id(({ "pickaxe" }));
    set_short("a pickaxe");
    set_long("A pickaxe to harvest ore resources with.");
    set_autoload(1);
}