inherit STD_ITEM;

void create () {
    ::create();
    set_name("prybar");
    set_id(({ "prybar" }));
    set_short("a prybar");
    set_long("A prybar to harvest salvage resources with.");
    set_autoload(1);
}
