inherit STD_ITEM;

void create () {
    ::create();
    set_name("museum ticket");
    set_id(({ "ticket", "museum ticket", }));
    set_short("a museum ticket");
    set_long("A paper ticket for the museum. It will admit one person entrance to the museum.");
}