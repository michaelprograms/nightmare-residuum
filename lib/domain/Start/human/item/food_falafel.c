inherit STD_FOOD;

void create () {
    ::create();
    set_name("falafel");
    set_id(({ "falafel" }));
    set_adjective(({ "crispy" }));
    set_short("a crispy falafel");
    set_long("A falafel.");
    set_strength(20);
    set_value(11);
}