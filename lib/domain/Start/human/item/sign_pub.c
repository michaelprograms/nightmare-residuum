inherit STD_ITEM;

void create () {
    ::create();
    set_name("sign");
    set_id(({"sign"}));
    set_short("a wooden sign painted with the word %^YELLOW%^PUB%^DEFAULT%^");
    set_long("A wooden sign outside the building on the northeast corner painted with the word PUB in bright yellow paint.");
    set_no_get(1);
}
