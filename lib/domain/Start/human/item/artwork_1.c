inherit STD_ITEM;

void create () {
    ::create();
    set_id(({"sculpture", "discs", "disc"}));
    set_name("abstract disc sculpture");
    set_short("an abstract disc sculpture");
    set_long("A series of flat discs intersecting with one another. One of the discs has a circular punch-out with its chad attached to its side. The sculpture looks different from every angle.");
    set_no_get(1);
}
