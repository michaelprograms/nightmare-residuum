inherit STD_ITEM;

void create () {
    ::create();
    set_name("bench");
    set_id(({"bench"}));
    set_short("a wooden bench");
    set_long("A wooden bench.");
    // @TODO set_no_get("You cannot get that.");
}
