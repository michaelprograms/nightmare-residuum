inherit STD_ITEM;

void create () {
    ::create();
    set_name("plum");
    set_id(({ "plum" }));
    set_adjective(({ "juicy" }));
    set_short("a juicy %^RESET%^MAGENTA%^plum");
    set_long("A plum.");
}