inherit STD_ITEM;

void create () {
    ::create();
    set_id(({"sculpture", "beams"}));
    set_name("abstract sculpture");
    set_short("an abstract beam sculpture");
    set_long("A tall structure of large, curved beams which intersect and overlap each other. The beams are painted with a bright red-orange color. The structure looks different from every angle.");
    set_no_get(1);
}
