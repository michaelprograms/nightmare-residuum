inherit STD_FOOD;

void create () {
    ::create();
    set_name("apple");
    set_id(({ "apple" }));
    set_adjective(({ "sweet" }));
    set_short("a sweet apple");
    set_long("An apple.");
    set_strength(5);
}