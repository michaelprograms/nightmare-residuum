inherit STD_FOOD;

void create () {
    ::create();
    set_name("peach");
    set_id(({ "peach" }));
    set_adjective(({ "fuzzy" }));
    set_short("a fuzzy peach");
    set_long("A peach.");
    set_strength(5);
}