inherit STD_ITEM;

void create () {
    ::create();
    set_name("obelisk");
    set_id(({"obelisk"}));
    set_short("a tall %^I_WHITE%^obelisk%^DEFAULT%^");
    set_long("An obelisk constructed of an unknown material with no visible texture. The flat white surface reflects light from above. It towers over everything in the vicinity. There are openings on each side of the obelisk.");
    set_no_get(1);
}
