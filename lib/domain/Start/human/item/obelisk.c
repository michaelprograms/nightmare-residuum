inherit STD_ITEM;

void create () {
    ::create();
    set_name("obelisk");
    set_id(({"obelisk"}));
    set_short("a tall %^BOLD%^WHITE%^obelisk");
    set_long("An obelisk constructed of an unknown material with no visible texture. The flat white surface reflects lights from above. It towers over everything in the vicinity.");
    set_no_get(1);
}
