inherit STD_ARMOR;

void create () {
    ::create();
    set_name("canvas shoes");
    set_id(({ "canvas shoes", "shoes" }));
    set_short("canvas shoes");
    set_long("A pair of flimsy canvas shoes.");
    set_type("boots");
    set_limbs(({ "left foot", "right foot" }));
}