inherit STD_ARMOR;

void create () {
    ::create();
    set_name("straw hat");
    set_id(({ "straw hat", "hat" }));
    set_short("straw hat");
    set_long("A set of flimsy straw hat.");
    set_type("hat");
    set_limbs(({ "head" }));
}