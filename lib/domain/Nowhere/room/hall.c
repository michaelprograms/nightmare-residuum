inherit STD_ROOM;

void create () {
    ::create();
    set_short("nowhere");
    set_long("A windowless chamber where every surface is white. The walls, floor, and ceiling emit a luminescence which cast no shadows.");
}